/*
Variables are containers for storing information.
var name = value;
name :- Required.
        The name of the variable.
        Must begin with a letter, or $, or _
        Names are case sensitive (y and Y are different)
        Reserved JavaScript words cannot be used as names
value :-Optional.
        A value to be assigned to the variable.
*/

//Create a variables (a,b) and assign the values.
var a ;//declare a variable
a = 10;//assign a value
var b = 20;//declare & assign 

console.log(a+b);//print 